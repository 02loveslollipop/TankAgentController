name: Deploy to Heroku

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allows manual triggering

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Heroku CLI
        run: curl https://cli-assets.heroku.com/install.sh | sh

      - name: Log in to Heroku Container Registry
        run: |
          echo $HEROKU_API | docker login registry.heroku.com -u _ --password-stdin
        env:
          HEROKU_API: ${{ secrets.HEROKU_API }}

      - name: Build and push auth_service
        run: |
          docker build -t registry.heroku.com/tank-auth-service/web ./auth_service
          docker push registry.heroku.com/tank-auth-service/web
          heroku container:release web -a tank-auth-service
        env:
          HEROKU_API: ${{ secrets.HEROKU_API }}

      # Add similar steps for other services as you build them, e.g.:
      # - name: Build and push backend
      #   run: |
      #     docker build -t registry.heroku.com/your-backend-app/web ./backend
      #     docker push registry.heroku.com/your-backend-app/web
      #     heroku container:release web -a your-backend-app
      #   env:
      #     HEROKU_API: ${{ secrets.HEROKU_API }}