name: Build Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/test-docker.yml

  deploy:
    needs: build
    uses: ./.github/workflows/deploy.yml
    secrets: inherit

